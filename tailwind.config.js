module.exports = {
  darkMode: 'class', // Enable dark mode based on a class instead of system preference
  theme: {
    extend: {
      fontFamily: {
        Gotham: ["Gotham", "sans-serif"], // Ensure fallback font
      },
    },
  },
  plugins: [],
};
